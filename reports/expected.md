# Expected CallLint results

Generated by `calllint scan .cursor/mcp.json` (pinned timestamp for determinism).
This is the contract this demo asserts; the CI workflow reproduces it on every push.

```text
CallLint scan
config: .cursor/mcp.json
result: BLOCK   (BLOCK 1 · UNKNOWN 1 · REVIEW 1 · SAFE 1)
────────────────────────────────────────────────────────────

SAFE  safe-local-project    —
  S1 Read-only utility · reproducibility HIGH · confidence high
  "safe-local-project" has no blockers observed (S1 Read-only utility).

  autonomous use: allow · manual approval: none · sandbox: none

REVIEW  review-unpinned-npx    SUPPLY
  S1 Read-only utility · reproducibility MEDIUM · confidence high
  "review-unpinned-npx" needs review. Risk: Supply Chain (S1 Read-only utility).

  • Package version is not pinned  (supply.unpinned-package, observed, confidence high)
      evidence: package = @modelcontextprotocol/server-git
      impact: The installed code can change between scans and runs, so this verdict may not match what actually executes.
      fix: Pin the package to an exact version, e.g. @modelcontextprotocol/server-git@1.0.0.

  reproducibility notes: Package version is not pinned

  autonomous use: warn · manual approval: recommended · sandbox: recommended

BLOCK  block-broad-filesystem    FILES  SUPPLY
  S2 Sensitive read · reproducibility MEDIUM · confidence high
  "block-broad-filesystem" is blocked. Risk: Files, Supply Chain (S2 Sensitive read).

  • [BLOCKER] Broad local filesystem access  (files.broad-path, observed, confidence high)
      evidence: args = /Users/username
      impact: An agent-triggered tool could read private local files outside the project.
      fix: Restrict filesystem access to the current workspace, e.g. ${workspaceFolder}.
  • Package version is not pinned  (supply.unpinned-package, observed, confidence high)
      evidence: package = @modelcontextprotocol/server-filesystem
      impact: The installed code can change between scans and runs, so this verdict may not match what actually executes.
      fix: Pin the package to an exact version, e.g. @modelcontextprotocol/server-filesystem@1.0.0.

  reproducibility notes: Package version is not pinned

  autonomous use: deny · manual approval: required · sandbox: required

UNKNOWN  unknown-remote-vendor    NETWORK
  S1 Read-only utility · reproducibility MEDIUM · confidence high
  "unknown-remote-vendor" could not be verified (insufficient evidence). Risk: Network (S1 Read-only utility).

  • Remote server source cannot be verified  (supply.unknown-remote, observed, confidence high)
      evidence: url = https://mcp.unknown-vendor-example.dev/sse
      impact: The server runs on an unverified remote endpoint. Its tools, permissions, and behavior cannot be inspected statically and may change at any time.
      fix: Use a verified first-party endpoint, or run the server from a pinned, inspectable package.

  reproducibility notes: Remote endpoint could not be verified

  autonomous use: deny · manual approval: required · sandbox: required
```
